// =============================================================================
// posts/mod.rs — 帖子存储子模块的入口声明
// 职责：声明 posts 目录下的子模块，使其对外可见
// 模块说明：
//   - post_store: 核心内存帖子存储 PostStore，按 user_id 索引帖子，
//     支持插入、删除、查询、自动清理等操作
// 边界情况说明：
//   - 若 post_store.rs 缺失，编译期报错
//   - pub mod 表示该模块对 crate 外部可见（如 main.rs、thunder_service.rs 引用）
// =============================================================================

// 帖子存储模块：PostStore 的核心实现
pub mod post_store;
